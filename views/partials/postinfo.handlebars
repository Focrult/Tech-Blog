<div class="card mb-3">
  <div class="card-header bg-dark text-white">
    <h4><a class="text-white" href="/post/{{id}}">{{title}}</a></h4>
    <p class="text-muted m-0">Posted by {{user.username}} on {{format_date created_at}}</p>
  </div>
  <div class="card-body">
    <p class="card-text">{{text}}</p>
  </div>
</div>